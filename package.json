{
  "main": "server/index.js",
  "scripts": {
    "build": "cd dreamer && npm run build",
    "install-client": "cd dreamer && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "npm": "6.14.4",
    "node": "12.18.0"
  }
}
